# meteorengine
Minimal and experimental Game Engine

# Project setup
1. Once you've cloned the project, make sure to install `cmake` and `conan` with `brew install cmake` and `brew install conan`
1. Then, run `./build.sh` to install all the dependencies and compile the project.